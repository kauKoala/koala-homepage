import React from "react";
import Section from "@/components/common/Section";

type Props = {};

const FirstSection = (props: Props) => {
  return (
    <Section>
      <div>코딩 테스트가 막막하다면</div>
      <div>프로그래밍을 더 잘 하고 싶다면</div>
      <div>문제를 푸는 성취를 즐긴다면</div>
    </Section>
  );
};

export default FirstSection;
